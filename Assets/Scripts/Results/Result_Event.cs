using System;
using System.Collections.Generic;
using System.Text;
public class Result_Event
{
    public string id_prepend = "";
    public string id_append = "";
    public string id_overwrite = "";
    
    // always inject doer to self

    // inject doer to doer -> check treat receiver as doer
    // inject receiver to receiver -> check treat doer as receiver -> if doer solo then treat doer as receiver?
    // inject actor 

    // let event scoping sort it out

    /*
     for example, in shower cumdeflation event, check every actor, and in
    $target$ scope all inflated actor
    $assistant$ scope all in actor except target

    but thats only for deflation.
    what if its a potential sexual assault scene? 
    -> we will do that check in the future, 
    where we implement ways to define the assistant's desire in the relationship


     */

}

